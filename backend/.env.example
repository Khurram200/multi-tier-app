# Copy to .env and adjust for local (non-Docker) development
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=your_postgres_password
DB_NAME=todo
DB_PORT=5432
PORT=5000

# Optional: restrict CORS to frontend origin
# ALLOWED_ORIGIN=http://localhost:3000
