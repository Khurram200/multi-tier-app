version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  todo-notify:
    lang: dockerfile
    handler: ./todo-notify
    image: todo-notify:latest
    environment:
      write_debug: true
